"""Backend adapters. Each adapter wraps a browser-control library into a
PageLike (see browser_skills.primitives). v1 ships playwright_raw; the
browser-use and stagehand adapters arrive in later milestones.

See ADR-006 for the cross-runtime portability rationale.
"""
